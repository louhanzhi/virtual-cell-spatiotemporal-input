# 04_drafts · 论文草稿

只有一个 `versions/` 目录。**每个版本一个文件夹（v1、v2…），文件夹内自带该版本对应的 `outline.md` 和 `draft.md`**——骨架与草稿绑在一起，避免顶层 outline 与某一版脱节。

```
04_drafts/
└── versions/
    ├── v1/
    │   ├── outline.md   # 本版骨架（章节结构 + 字数配比）
    │   └── draft.md     # 本版正文
    └── v2/ …
```

写新版本时复制上一版文件夹改名即可。
